interface:
  display_name: "Qhash Python"
  short_description: "Repo-specific patterns and workflows for qhash-python"
  default_prompt: "Use the qhash-python repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true